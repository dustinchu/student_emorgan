double line_page1_left_width_size(double w) {
  if (w > 220)
    return w / 2.5;
  else if (w > 200)
    return w / 2.4;
  else if (w > 180)
    return w / 2.3;
  else if (w > 160)
    return w / 2.2;
  else if (w > 140)
    return w / 2;
  else if (w > 120) return w / 1.8;

  return w / 1.6;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}

double line_page1_width_right1_size(double w) {
  if (w > 260)
    return w / 3.1;
  else if (w > 230)
    return w / 3.2;
  else if (w > 200)
    return w / 3;
  else if (w > 170) return w / 2.9;

  return w / 2.8;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}
double line_page1_width_right2_size(double w) {
  if (w > 260)
    return w / 2.7;
  else if (w > 230)
    return w / 2.8;
  else if (w > 200)
    return w / 2.7;
  else if (w > 170) return w / 2.2;
    else if (w > 150) return w / 2;

  return w / 1.8;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}

double line_page2_left_width_size(double w) {
  if (w > 360)
    return w / 1.35;
  else if (w > 280)
    return w / 1.3;
  else if (w > 250)
    return w / 1.25;
  else if (w > 220)
    return w / 1.2;

  return w / 1.1;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}
double line_page2_left2_width_size(double w) {
  if (w > 300)
    return w / 1.45;
  else if (w > 230)
    return w / 1.4;
  else if (w > 200)
    return w / 1.35;
  return w / 1.3;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}
double line_page2_right1_width_size(double w) {
  if (w > 300)
    return w / 1.45;
  else if (w > 230)
    return w / 1.4;
  else if (w > 200)
    return w / 1.35;
  return w / 1.3;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}double line_page2_right2_width_size(double w) {
  if (w > 300)
    return w / 1.85;
  else if (w > 190)
    return w / 1.9;
  return w / 1.85;
  // else if (w > 325) return 198;

  // else if (w > 500) return 15;
  // return 10;
}